# Project Memory

## Stable Facts
- Repository working directory: `D:\for code\contractor`.
- GitHub remote `origin`: `https://github.com/nightperv162-png/contractor.git`.
- Local project folder is `Dragon Contractor`.
- Previous tracked project folder was `Dragon tractor`; current local commit records this as a pure directory rename.
- GitHub default branch is `main`.
- Local branch is `master`.

## Decisions
- Do not force-update GitHub `main` without explicit user approval because `origin/main` contains remote-only files that are absent locally.
- Build output in `Dragon Contractor/dist` was not staged in the folder rename commit.

